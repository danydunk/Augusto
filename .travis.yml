language: java
install: true

matrix:
    fast_finish: true
env:
    - CMD=first 
    - CMD=second    
    - CMD=third
    
script: travis_wait 300 python CI-client.py $TRAVIS_COMMIT $CMD

jdk:
  - oraclejdk8